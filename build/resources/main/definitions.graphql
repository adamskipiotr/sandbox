type Query {
    bookById(id : ID): Book
    author(id: ID): Author
}

type Book {
    id: ID
    name: String!
    chapters: [String]
    pageCount: Int
    author: Author
}

type Author{
    id: ID
    firstName: String
    lastName: String
}